# K0V1DH
This is my repository for the projects from manas taskphase
Command for clone: git clone "https link"
to change directory or basically access the folder inside press "cd foldername"
"ls" for listing files
"ls -a" to access hidden files if your terminal is gitbash if powershell then type "ls -hidden"
"git status" for status to know if whats modified and all not that important
after modifying a file in vscode you have to add it then commit it for it to be registered in git
untracked files are new files that git bro has no clue about and then modified is bro knows the file but u changed it
staged files are files that are added but not yet commited, engaged but not married
"git add+(file name)" to add the file and "git add ." to add all files
"git commit -m (message)" to commit a file with a message